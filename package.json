{
  "name": "static-website-cicd",
  "version": "1.0.0",
  "description": "Static website with CI/CD pipeline",
  "scripts": {
    "test": "echo 'Running unit tests...' && exit 0",
    "build": "echo 'Building static website...' && cp -r app/public dist/",
    "start": "npx http-server app/public -p 3000",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "cypress": "^12.0.0",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
